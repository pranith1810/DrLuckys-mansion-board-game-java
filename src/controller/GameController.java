package controller;

/**
 * An interface that represents the controller of the game which is used for
 * starting the game.
 */
public interface GameController {
  /**
   * A method which is used to start the game by displaying the first window.
   */
  public void startGame();
}
