package controller;

/**
 * An enum to represent all the commands provided by the model.
 */
public enum Command {
  MOVE, PICK_ITEM, LOOK_AROUND, ATTACK_TARGET, MOVE_PET, ADD_PLAYER, DISPLAY_SPACE_INFO, DRAW_IMAGE,
  IS_GAME_OVER, PERFORM_COMPUTER_ACTION, SET_WORLD, IS_CURRENT_COMPUTER;
}
